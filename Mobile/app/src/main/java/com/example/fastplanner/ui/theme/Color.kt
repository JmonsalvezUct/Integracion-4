package com.example.fastplanner.ui.theme

import androidx.compose.ui.graphics.Color

// ===== Paleta base existente (se mantienen) =====
val BluePrimary       = Color(0xFF2A36FF) // color principal (fondo y botón)
val BluePrimaryLight  = Color(0xFF9AA2FF) // variante clara (containers, estados)
val OutlineGray       = Color(0xFFE0E0E0) // bordes de inputs/cards
val TextPrimary       = Color(0xFF1B1B1B) // texto principal sobre superficies claras
val TextSecondary     = Color(0xFF7D7D7D) // texto secundario / descripciones

// Colores específicos de tableros / MainScreen (se mantienen)
val BoardBlue   = Color(0xFF2F8BFF)
val BoardGreen  = Color(0xFF4BC19D)
val BoardPink   = Color(0xFFF06AB1)
val BoardRed    = Color(0xFFEF6A6A)

// Layout heredado (se mantienen como referencia para claro)
val HeaderColor     = Color(0xFF3E21FF)  // header morado
val ContentBgColor  = Color(0xFFF1F5F9)  // fondo gris claro

// ===== Extensiones para modo oscuro =====
val BackgroundDark        = Color(0xFF0E0F12)
val SurfaceDark           = Color(0xFF1A1B22)
val TextPrimaryDark       = Color(0xFFE5E7EB)
val TextSecondaryDark     = Color(0xFF9CA3AF)
val OutlineVariantDark    = Color(0xFF2D2F36)
val PrimaryContainerDark  = Color(0xFF3B3563)


